# Changelog

## 0.1.0.0

- Initial spike: `Cursor` with `newMem` (IORef) and `newFile` backends
- `pollLines` / `pollFile` — what is new since I last asked
- `seekEnd` / `seekEndFile` — attach at tail
